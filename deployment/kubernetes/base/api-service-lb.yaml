apiVersion: v1
kind: Service
metadata:
  name: api-service-lb
  namespace: house-price-predictor
  labels:
    app: api-service
spec:
  type: LoadBalancer
  selector:
    app: api-service
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
